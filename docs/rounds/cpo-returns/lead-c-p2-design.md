# Lead C (platform) phase-two return, relayed by Fable (main session)

TASK: CDQO-LC / attempt 1 / rev 1 + four addenda. STATUS: submitted. 29 commits on main: 148f836, 8d4d5e1, 4c2a8e2, 4242fec, 2b59703, a74d14b, 42b75e0, ba0df7c, c804550, bc881af, d14effc, 928fb8f, 7343c7d, 5c1fc4a, 3d97686, c167dc1, 1db875c, f25e586, 1d3763f, 383ee27, 264a6f9, d2b9d72, da1003e, 46a9eae, 6dd2233, b1846c2, ff5f59f, b905136, f15d721.

Findings, all implemented: D1 VirtualList fit measured from an overflow:hidden panel (hid 6 of 16 contacts) → measure only from a scrolling ancestor, clamp to viewport, re-measure after layout (1d3763f); D2 Settings > Workspace one save model (7343c7d); D3 repeated explanations removed on Backups, import file step, Export; D4 one overlay system (src/ui/Overlay.tsx; palette rewritten on it, 4242fec); D5 dialog opened by key returns focus (2b59703, e2e f15d721); D6 table rules in the kit (TBody last hairline, TableScroll sticky header, TD dashZero + moneyOrDash, 8d4d5e1); D7 icon size canon in icons.ts, aria-label everywhere with ariaLabel deprecated; D8 DatePicker aria-invalid border, Kbd accessible name (b1846c2); D9 boot screens fold raw JS behind a disclosure with copy (3d97686); D10 Help headings, duplicates naming, settings caret. Deferred: none real (ImportScreen "breadcrumb" is a button icon at the correct 16).
Sequence C: main chunk 3,040 → 1,355 kB raw, 886 → 358 kB gzip; lazyScreen helper + shell Suspense; jszip/papaparse split.

CONTRACTS text to place (verbatim in the return): route-level splitting via lazyScreen (never on the boot path; no eager re-export of a lazy screen); VirtualList fit semantics (scrolling ancestor only, data-fit, no flex-1 on the surface); labels (aria-label canonical, ariaLabel deprecated alias); vocabulary pure table in src/lib/vocabulary.ts re-exported by src/app/vocabulary.ts. DESIGN.md §4 already carries the PDF-fonts sentence.

Verification: tsc clean; unit/repo 68 files / 1005 tests in its areas; e2e data|settings|smoke|onboarding|ai|hig 63 passed and listNav+records 28 passed on 4251; vite build clean; greps clean; 86 + 42 screenshots under screens/lc/; design specs deleted with assertions moved; nothing running.
Deviations: shipped then fixed D1 in the same round; overrode two W1 calls; W3's 1db875c lacks the attribution trailer (not amended); one pre-existing max-h-[220px] on the boot error blob.
