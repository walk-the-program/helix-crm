# Lead A phase-two (design) return, relayed by Fable (main session)

TASK: CDQO-LA / attempt 1 / rev 1. STATUS: submitted.

Findings implemented (commits 1048329, 8e1f096, ddcf0f8, 3ed1145, cfcbff7): F-LA-D1 "No company" → muted em dash; D2 board subtotals drop "Upfront" unless monthly exists; D3 deals list shows Customer like the board; D4 vocabulary in stage manager header and Companies empty state; D5 empty lists hide the toolbar until there is data (filters that match nothing keep it); D6 board empty state "Add your first job"; D7 corrected its own phase-one change: expected close/won date back in the deal header (Identity card is the only editor); D8 contact rail leads with jobs; D9 whole-row links not underlined as fragments; D10 stage dialog button "Move to <Stage>" (one-line selector fix routed to revenue.e2e.ts); D11 moveToStage validation reads vocabulary (zod newDealSchema messages deferred: fixed at module load); D12 empty white slab under short lists closed by lead-platform's VirtualList fit + 1d3763f, adopted; guard test cfcbff7 (5/5 on HEAD).
Workers: W1 (Tasks, Trash, Reminders, Templates): two CRITICAL reachability bugs at 1024 (Trash's 13 tabs ran off-screen with no scroll: 62a5067; Reminders action column clipped: 0761248), disabled Delete-forever explanation, RuleDialog validation timing, Templates merge-field glossary (0c0ccd0). W2 (Today, quick add, search): tag-before-name on Today rows (9c6c104), standing sentences → tooltips, five "deal" strings, quick add tablist keyboard navigation (866d8c1), errors persisting while retyping, search dialog on the kit overlay so ⌘K and ⌘⇧K match (2b5c627).
Raised, not patched: DatePicker accepts aria-invalid without a border style; Kbd has no accessible label; search GROUP_HEADINGS.deal ignores vocabulary; {{deal_title}}/{{deal_value}} tokens need a back-compat decision; harness re-applies theme/density on mount (coordinator fixed cpoWalk in da08d95).
Self-correction: an earlier "main hides 10 of 16 records" alarm was its own working-tree measurement; guard rewritten and green.
Verification: typecheck clean; vitest 154 files / 2094 passed / 3 skipped; records+today+depth+listNav e2e 56 passed on 4231; build folders removed; audit specs deleted (assertions moved).
Handoff: uncommitted TodayScreen.tsx / ConnectSite.tsx in the tree are lead-platform's copy change (needs an owner). W1's 3ed1145 was a `git commit --amend` on a shared index that swept four Lead A files under a Trash message (content intact; 3ee7a3d not on main as a result); left alone per the standing rule. Attribution trailers are mixed (Sonnet vs Opus lines).
